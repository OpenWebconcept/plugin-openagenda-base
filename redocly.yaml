apis:
  main@v1:
    root: ./openapi.yaml
    extends:
      - minimal
rules:
  no-unused-components: error
theme:
  openapi:
    htmlTemplate: ./docs/index.html
    theme:
      colors:
        primary:
          main: "#32329f"